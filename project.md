# ZeiFlow プロジェクト全体像

## 1行で言うと
**税理士事務所のための、レシートOCR + 仕訳変換 SaaS** です。

## 誰が使う？
- 🏢 **税理士事務所**（メインユーザー）
- 👤 **その事務所の顧問先（クライアント企業）** ← 顧客ポータル経由でレシートを送る

## 何を解決する？
- 📥 紙のレシートを手入力する手間をなくす
- 🧾 インボイス制度に対応した正しい仕訳を作る
- 🔄 弥生・freeeなど会計ソフトに取り込めるデータを出力する

## 主な機能（ダッシュボードのメニュー）
| 機能 | 役割 |
|------|------|
| 📸 receipts（レシート） | OCRでレシート画像を読み取る |
| 👥 clients（顧客） | 顧問先の企業情報を管理 |
| 📊 journals（仕訳） | 会計仕訳の作成・編集 |
| 📤 export（エクスポート） | 会計ソフト向けデータ出力 |
| 📥 import（インポート） | データ取り込み |
| 🔒 portal（ポータル） | 顧問先がレシートを送るための窓口 |
| 📚 knowledge（ナレッジ） | AI用の参考資料を登録 |
| 📝 audit（監査ログ） | 誰が何をしたかの記録 |
| 🏢 org（組織） | 事務所のメンバー管理 |
| ⚙️ settings（設定） | 各種設定 |
| 👑 superadmin | 全体管理者用画面 |

## 技術スタック（裏側のしくみ）
- **フレームワーク**：Next.js 16（独自カスタム版・AGENTS.md 参照）
- **言語**：TypeScript
- **データベース**：PostgreSQL（Prisma経由）
- **AI**：Anthropic Claude API（OCR処理）
- **認証**：NextAuth v5 + bcrypt + 2要素認証(TOTP)
- **メール**：Resend
- **UI**：shadcn/ui + Tailwind CSS 4
- **デプロイ先**：Vercel

## ファイルの場所（どこに何がある？）
```
zeiflow/
├── src/
│   ├── app/              ← 画面（ページ）が全部ここ
│   │   ├── dashboard/    ← ログイン後の画面
│   │   ├── api/          ← サーバー側の処理
│   │   ├── login/        ← ログイン画面
│   │   ├── register/     ← 新規登録
│   │   └── portal/       ← 顧問先用ポータル
│   ├── components/       ← 部品（ボタン、フォームなど）
│   ├── lib/              ← 共通の関数
│   └── generated/prisma/ ← DB操作の自動生成コード
├── prisma/
│   └── schema.prisma     ← データベース設計図
└── public/               ← 画像などの静的ファイル
```

## デプロイ
- **本番URL**：Vercel上で稼働中（デスクトップの `Zeiflow.url` 参照）
- **管理者URL**：デスクトップの `Zeiflo管理者.url` 参照

## ビジネス上のポイント
- インボイス制度（2023年10月開始）への対応が必須
- 税理士事務所 → 顧問先への請求が発生する想定（プランあり：free / 有料）
- 顧問先は「ポータルトークン」で簡単にレシートを送れる
