# 不具合リスト（bugs.md）

最終更新：2026-07-28

## 🔴 現在判明している不具合

- [ ] **既存API /api/receipts/retry が取込確認済みレシートでも仕訳を消してOCRし直せる(2026-07-26 レビューbot指摘・既存課題)**
  - UIからは押せない・API直叩き限定。確認済みステータスのガードを追加すべき
  - 影響: 低(認証済みユーザーの自傷のみ)だが、確定データの意図しない消失経路

- [ ] **一括確定(bulk-update)が未編集の出力済み仕訳の updatedAt も更新する(2026-07-26 レビューbot指摘・既存課題)**
  - 「出力済みを除外」判定が updatedAt 更新で外れて二重計上し得る(旧ロジックも同性質=退行ではない)
  - 恒久策: 値が変わらない no-op 更新のスキップ

- [ ] **パスワードリセットメールが実顧客に届かない（2026-07-07 本番実測で確定）**
  - 原因: Resend が**テストモード**（送信元 `noreply@resend.dev`、RESEND_FROM 未設定）のため、Resendアカウントオーナー(keita.030312@gmail.com)宛て以外は送信拒否される
  - 実測: +エイリアス宛リセットメールは3分以上未配達。同時刻のオーナー宛「事務所登録通知」は数秒で配達 → Resend自体は稼働
  - APIは送信失敗を握りつぶして success を返す仕様（列挙攻撃対策としては正しい）ので、顧客側は「メールが来ない」だけで気づけない
  - **修正方法（コード変更不要）**: Resendで独自ドメインをDNS検証 → Vercelの `RESEND_FROM` に `ZeiFlow <noreply@検証済みドメイン>` を設定
  - それまでの運用: 顧客からリセット依頼が来たら superadmin で手動対応

- [ ] **CSV出力済みの顧問先が削除できない(2026-07-26 納品前チェックE2Eで発見)**
  - 顧問先削除時に「先にエクスポート履歴を削除してください」と表示されるが、**一般ユーザー向けのエクスポート履歴削除機能はUI/APIとも存在しない**(superadminの組織削除カスケードのみ)
  - 一度でもCSV出力した顧問先は事実上削除不能。エラー文言と実機能が矛盾
  - 対応案: ①エクスポート履歴の削除機能を作る ②顧問先を「アーカイブ(非表示)」方式にする ③エラー文言を実態に合わせる — 製品判断待ち
  - 該当: `src/app/api/clients/route.ts:211-218`

- [ ] **POST /api/export に壊れたJSONを送ると500+内部エラー文言がdetailに露出(低・既存コードの挙動)**
  - 400が適切。JSON.parse失敗を捕捉してzodと同じ400系に寄せる
  - セキュリティレビュー判定: 秘密情報の漏洩はなし(重大度低)

## 🟡 要確認（バグかも？）

- [ ] **OCRが読めないレシートがある時の挙動**
  - エラーで止まる？空欄になる？
  - ユーザーに分かるように表示されるか

- [x] **大きい画像ファイルのアップロード** — 2026-07-07 検証済み
  - UI: 選択時に「10MB以下の画像を選択してください」トーストで弾く（receipts/portal両方）✓
  - API直叩き: Vercelインフラが 413 FUNCTION_PAYLOAD_TOO_LARGE で拒否（アプリの手前）
  - 既知の制限: 4.5MB超のリクエストボディはVercel関数制限で413になる。UIはアップロード前に圧縮(image-compress)するため実運用では実質問題なし

- [ ] **同時に複数人がログインした時**
  - 監査ログが正しく記録されるか
  - データが混ざらないか

- [ ] **2FA設定後、リカバリー手段はあるか** — 2026-07-07 一部確認
  - ログインフロー自体は正常（コード無し→requires2FA / 誤コード→401 / 正コード→ログイン成功、UI側はprompt入力）✓
  - **リカバリー手段は現状なし**（DELETE /api/settings/totp はログイン済み前提）。機種変で詰む → superadminでDBの totp_enabled を落とす運用 or リカバリーコード実装を検討

- [ ] **パスワードリセットメールが届かないケース**
  - → 🔴欄に昇格（Resendテストモードが原因と確定）

## ✅ 修正済み

- [x] **弥生会計 CSV 文字コードエラー・T列(タイプ)/Y列(調整)空欄** — 2026-06-21
  - 文字コードがUTF-8のため弥生側でANSI変換が必要だった → Shift-JIS出力に変更(iconv-lite使用)
  - T列(タイプ)・Y列(調整)が空欄でインポート不可だった → それぞれ `"0"` を設定
  - **注意: 修正は6/21にコミット済みだったが本番未デプロイのまま7/7に顧客から再報告** → 2026-07-07 vercel --prod で本番反映し、本番の実CSVバイト列(Shift-JIS先頭 `8eaf95ca`・25列・T/Y="0")を確認済み。教訓: 顧客報告バグの修正は「コミット」でなく「デプロイ」までがクローズ条件
  - **レビューで発見・修正**: `"0"` 設定時に番号・期日の2列を誤って削除し列数が23→25にずれていた → 番号・期日を復元して全25列を正しく配置
  - **レビューで発見・修正**: `encoded.buffer` がNode.jsメモリプール全体(8KB)を返す可能性 → `new Uint8Array(encoded).buffer` で独立コピーに変更

- [x] **弥生会計 CSV インポートエラー（「インポート可能な形式ではない」）** — 2026-06-14
  - 摘要/メモ内の `\r\n` が行分割されCSV行数がずれていた → `[\r\n]+` をスペースに置換
  - 列名「借方金額」→「借方金額(税込)」「貸方金額」→「貸方金額(税込)」に修正
  - 識別フラグを数値 `2000` → 文字列 `"2000"` に修正
  - タイプフィールド `0` → `""` に修正
  - 調整フィールド `"NO"` → `""` に修正
  - 借方税金額を未設定時に `0` → `""` に修正

- [x] **MoneyForward MF仕訳タイプが「開始仕訳」になっていた** — 2026-06-14
  - 通常仕訳なのに `"開始仕訳"` が入っていた → `"通常仕訳"` に修正

- [x] **エクスポート periodEnd が翌日 00:00 になる** — 2026-06-14
  - ExportLog の periodEnd が midnight 保存で、最終日の仕訳が「出力済み」判定から漏れていた
  - → 23:59:59.999 で保存するよう修正

- [x] **2FA bypass（login-form 経由）** — 2026-05-30
  - `/api/auth/login-form` を叩くことで 2FA を迂回できた
  - → TOTP 検証を form login 経由にも追加済み

- [x] **仕訳 API のテナント越境アクセス** — 2026-05-30
  - 他テナントの `clientId`/`receiptId` を指定して仕訳操作が可能だった
  - → 所有確認を追加・PUT の更新フィールドを zod schema で制限済み

- [x] **一括確定（bulk-confirm）のスコープ漏れ** — 2026-05-30
  - レシート参照解除・削除が `getScope` の外で実行されていた
  - → `getScope(auth)` の範囲内に制限済み

- [x] **ポータル認証 組織ゲート欠如** — 2026-05-30
  - 組織停止中・承認待ちでもポータルアクセスが可能だった
  - → `checkOrgGate()` をポータル認証に適用済み

- [x] **ポータル OCR 空結果で COMPLETED になる** — 2026-05-30
  - OCR が 0 件を返してもアップロードが成功扱いになっていた
  - → 空結果は失敗扱いに変更済み

---

## バグを見つけたときの書き方

```
- [ ] **何が起きるか**（1行で）
  - 再現手順：何をしたら起きる？
  - 期待する動き：本来どうなってほしい？
  - 発生環境：PC/スマホ、ブラウザ、いつ気づいた？
```

## 修正したら
1. このファイルで `[ ]` → `[x]` に変更
2. 「✅ 修正済み」セクションに移動
3. progress.md にも反映
